<?php

return [

    /*
     * The locales your application supports.
     *
     * Each locale must exist in the `locales` array below.
     */
    'locales' => [
        'ar',
        'en',
    ],

    /*
     * The fallback locale determines which translation to use when the
     * requested translation does not exist. Set it to your default language.
     */
    'fallback_locale' => 'ar',

    /*
     * If a translation has not been set for a given locale, this locale will be used.
     */
    'fallback_for_missing_translations' => true,

];